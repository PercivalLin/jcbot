{
  "name": "@lobster/policy",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./dist/policy/src/index.js",
  "types": "./dist/policy/src/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --exclude '**/._*'",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@lobster/shared": "workspace:*",
    "yaml": "^2.7.0"
  }
}
