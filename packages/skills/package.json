{
  "name": "@lobster/skills",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./dist/skills/src/index.js",
  "types": "./dist/skills/src/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --exclude '**/._*'",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@lobster/policy": "workspace:*",
    "@lobster/shared": "workspace:*"
  }
}
